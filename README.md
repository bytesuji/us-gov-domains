# List of all .gov and .mil domain names

JSON file containing all .gov and .mil domains. A relatively complete index of the web properties of the US government.

## Structure
In `domains.json`:
```
{
    "gov": [...], // .gov domains
    "mil": [...], // .mil domains
}
```
